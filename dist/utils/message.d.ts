import type { TClassStyles, TMessage } from '../types/global';
export declare const messageClassAndText: (message: TMessage, MessageTypes: TClassStyles) => {
    classText: string;
    messageText: string;
};
