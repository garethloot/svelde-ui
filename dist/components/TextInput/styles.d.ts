import type { TMessage, TSize } from '../../types/global';
import type { TTextInputColors } from './types';
export declare const createClass: (color: TTextInputColors, size: TSize, bordered: boolean | undefined, message: TMessage) => string;
